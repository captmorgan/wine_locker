
<table>
  <tr>
    <th>Taster</th>
    <th>Producer</th>
    <th>Year</th>
    <th>Grape</th>
    <th>Rating</th>
    <th>Tasting Date</th>
    <th>Price</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @wines.each do |wine| %>
  <tr>
    <td><%= wine.name %></td>
    <td><%= wine.producer %></td>
    <td><%= wine.year %></td>
    <td><%= wine.grape %></td>
    <td><%= wine.rating %></td>
    <td><%= wine.tasting_date %></td>
    <td><%= wine.price %></td>
    <td><%= link_to 'Show', wine %></td>
    <td><%= link_to 'Edit', edit_wine_path(wine) %></td>
    <td><%= link_to 'Destroy', wine, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Wine', new_wine_path %>
</div>

</div>
